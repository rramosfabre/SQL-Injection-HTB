 In the 'titles' table, what is the number of records WHERE the employee number 
 is greater than 10000 OR their title does NOT contain 'engineer'?
 
 use employees;
 
 show tables;
 
 describe titles;
 
 select * from titles where title != "Engineer" or emp_no > 10000;
 
 654
